services:
  portfolio_website:
    build:
      context: .
    image: portfolio_website
    expose:
      - "8081"
    restart: unless-stopped
    networks:
      - cnn_network
    pull_policy: never
    container_name: portfolio_website

networks:
  cnn_network:
   external: true