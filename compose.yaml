services:
  portfolio_website:
    build:
      context: .
    image: portfolio_website
    expose:
      - "8081"
    restart: unless-stopped
    networks:
      - portfolio_network
    pull_policy: never
    container_name: portfolio_website

networks:
  portfolio_network:
   external: true